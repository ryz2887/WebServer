# 编译产物
# ⚠️ 必须写成 /server(带前导斜杠 = 只匹配仓库根目录):
#    裸写 server 会匹配任意层级叫 server 的文件或目录,把 code/server/ 整个目录
#    (epoller.h/.cpp、webserver.h/.cpp)也一起忽略掉 —— 那些是源码,必须进仓库!
/server
*.o
*.exe
*.out

# 日志
*.log

# 临时目录
test_log_tmp/

# 编辑器
.vscode/
.idea/

# IDE 配置
*.code-workspace
